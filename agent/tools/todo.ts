import { todo } from "eve/tools/defaults";

/**
 * Durable todo list for tracking tasks across turns.
 *
 * Re-exports eve's built-in todo tool. The state is persisted by the framework.
 */
export default todo;
